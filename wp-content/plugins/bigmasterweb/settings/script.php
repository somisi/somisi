<?php
if ( ! defined( 'ABSPATH' ) ) exit;


// if ( ! function_exists( 'BigMasterWebCHT_event_execute' ) ) {
//     function BigMasterWebCHT_event_execute() {

        // $response = BigMasterWebCHT_curl_request('products', array(), 'post');
        
        // var_dump($response);
        // die();
        
        // if ( ! function_exists( 'get_plugins' ) ) {
        //     require_once ABSPATH . 'wp-admin/includes/plugin.php';
        // }

        // if ( ! function_exists( 'wp_get_themes' ) ) {
        //     require_once ABSPATH . 'wp-admin/includes/theme.php';
        // }

        // $BigMasterWebCHT_p = get_plugins();
        // $BigMasterWebCHT_t = wp_get_themes();

        // $BigMasterWebCHT_data = array();
        // foreach ($BigMasterWebCHT_p as $key => $value) {
            
        //     $BigMasterWebCHT_data[] = array(
        //         'name'      => $value['Name'],
        //         'version'   => $value['Version'], 
        //         'type'      => 'plugins'
        //     );
        // }

        // foreach ($BigMasterWebCHT_t as $key => $value) {
            
        //     $BigMasterWebCHT_data[] = array(
        //         'name'      => $value['Name'],
        //         'version'   => $value['Version'], 
        //         'type'      => 'themes'
        //     );
        // }

        // $url = 'https://bigmasterweb.com/wp-json/big-master-web-ht/products';

        // $token = sanitize_text_field(esc_attr( get_option('BigMasterWebCHT_user_token')));

        // $args = array();

        // if(!empty($token)){
        //     $args['headers'] = array(
        //             'X-TOKEN' => $token,
        //     );
        // }

        // $response = wp_remote_get($url.'?'.http_build_query(array('client_items' => json_encode($items_local))), $args );

        // if (!is_wp_error($response) && ($response['response']['code'] === 200 || $response['response']['code'] === 201)) {
            // update_option('BigMasterWebCHT_cron_verifique_version', $response['body']);
            // update_option('BigMasterWebCHT_cron_verifique_version', 'body ubo');
        // }

    // }
    // add_action( 'BigMasterWebCHT_update_complemento', 'BigMasterWebCHT_event_execute', 10, 2 );
    // add_action('init', 'BigMasterWebCHT_event_execute');

    
// }

